# Functional Test Cases — moved

The catalogue has been split per section. See
[`docs/test-cases/`](./test-cases/) for the per-module files and the
[README](./test-cases/README.md) for the index + coverage map.
