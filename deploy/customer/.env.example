# Copy to .env on the server.
# Note: the API URL is BAKED INTO the JS bundle at build time via
# VITE_API_URL / VITE_WS_URL — env vars set here only affect the
# static server itself (PORT).
PORT=5174
